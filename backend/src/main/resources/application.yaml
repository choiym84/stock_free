spring:
  application:
    name: backend
  docker:
    compose:
      lifecycle-management: start-and-stop
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
